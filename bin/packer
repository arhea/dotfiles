#!/bin/bash

docker run -i --rm --name packer \
    --pid host \
    -w /usr/src/app \
    -v $HOME/.aws:/root/.aws:ro \
    -v $HOME/.ssh:/root/.ssh:ro \
    -v $(pwd):/usr/src/app \
    hashicorp/packer:light "$@"
