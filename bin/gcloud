#!/bin/bash

docker run --rm -i --name gcloud \
		-v $HOME/.gcloud:/root/.config/gcloud \
		-v $HOME/.ssh:/root/.ssh:ro \
		-v $(which docker):/usr/bin/docker \
		-v /var/run/docker.sock:/var/run/docker.sock \
		jess/gcloud:latest "$@"
