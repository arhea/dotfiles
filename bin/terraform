#!/bin/bash

docker run -i --rm --name terraform \
    --pid host \
    -w /usr/src/app \
    -v $HOME/.aws:/root/.aws:ro \
    -v $HOME/.ssh:/root/.ssh:ro \
    -v $(pwd):/usr/src/app \
    hashicorp/terraform:light "$@"
