#!/bin/bash

unset DOCKER_HOST
unset DOCKER_TLS_VERIFY
unset DOCKER_CERT_PATH

docker pull arhea/awscli:latest > /dev/null

docker run -i --rm \
  -v $HOME/.aws:/root/.aws:ro \
  -v $HOME/.ssh:/root/.ssh:ro \
  -v $(pwd):/usr/src/app \
  -w /usr/src/app \
  arhea/awscli:latest "$@"
